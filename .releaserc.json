{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "/usr/libexec/PlistBuddy -c \"Set CFBundleShortVersionString ${nextRelease.version}\" Boo/App/Info.plist && make release app && ([ -n \"$SIGNING_IDENTITY\" ] && make sign || codesign --force --deep --sign - .build/Boo.app) && ([ -n \"$APPLE_ID\" ] && make notarize || true) && make dmg"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["Boo/App/Info.plist"],
        "message": "chore(release): ${nextRelease.version} [skip ci]"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": ".build/Boo-*.dmg",
            "label": "Boo-${nextRelease.version}.dmg"
          }
        ]
      }
    ]
  ]
}
